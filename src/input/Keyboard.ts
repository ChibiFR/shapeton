export abstract class Keyboard {
  // protected static keys: Key[];

  // public static _init(): void {
  //   this.keys = [];

  //   window.addEventListener('keydown', (e: KeyboardEvent): void => {
  //     const index = this.keys.indexOf(e.keyCode);

  //     if (index !== -1) {
  //       this.keys[index] = true;
  //     }
  //   });
  // }
}
